# FT-002: 阶段自动识别与上游恢复

## 基本信息
- **Feature ID**: FT-002
- **功能名称**: 阶段自动识别与上游恢复
- **对应需求**: REQ-002, REQ-003
- **功能类型**: 核心能力
- **优先级**: 必须
- **状态**: 待实现

## D-01: 功能标识
- **Feature ID**: FT-002
- **入口类型**: Skill Internal Function
- **入口路径**: `skills/pb-review-v2/` (内部函数)

## D-02: 输入规格
| 参数名 | 类型 | 必填 | 约束 | 默认值 | 示例 |
|--------|------|------|------|--------|------|
| iteration_dir | string | 是 | 有效迭代目录 | - | `docs/iterations/014-*/` |

## D-03: 前置条件
- 迭代目录存在
- 至少包含一个 ASP 文档

## D-04: 正常输出
```yaml
stage: enum [product, spec, architecture, plan, implementation]
review_target: string  # 主审查对象
upstream_chain: array  # 上游产物链
  - file: string
    exists: boolean
    role: string
```

## D-05: 异常行为
| 错误码 | 触发条件 | 行为 |
|--------|---------|------|
| ERR-STAGE-001 | 无法识别阶段 | 返回 null，由调用方处理 |
| ERR-STAGE-002 | 上游产物缺失 | 标记 exists=false，继续 |

## D-06: 边界值
- 仅有 proposal.md → product 阶段
- 有 proposal + feature-specs → spec 阶段
- 有 architecture.md → architecture 阶段

## D-07: 后置条件
- 阶段识别结果可被后续流程使用
- 上游链完整性已检查

## D-08: 副作用
- 无副作用（只读操作）

## D-17: Test Oracle
**成功输出 Schema**:
```yaml
stage: string
review_target: string
upstream_chain: array
```

## D-18: Fixture Contract
**最小数据集**:
- 5 个测试迭代目录，分别代表 5 个阶段

## D-19: Test Case Groups
| 测试组 | 用例数 | 优先级 |
|--------|--------|--------|
| 阶段识别 | 5 | P0 |
| 上游恢复 | 5 | P0 |
| 缺失处理 | 3 | P1 |

## D-20: Coverage Claim
- **阶段识别覆盖**: 100% (5/5)
- **上游恢复覆盖**: 100%

## D-09: 性能要求
- **响应时间**: 阶段识别 < 1 秒（基于文件存在性检查）
- **吞吐量**: 单次调用
- **资源消耗**: 极低（仅文件系统 stat 操作）

## D-10: 安全要求
- **数据隐私**: 不上传用户文档
- **权限控制**: 只读取迭代目录结构
- **输入验证**: 验证迭代目录路径有效性

## D-11: 并发要求
- **并发模式**: 无并发需求
- **冲突处理**: 不适用

## D-12: 数据一致性
- **幂等性**: 同一目录结构重复识别产生相同结果
- **一致性保证**: 识别结果与目录实际内容一致

## D-13: 可观测性
- **日志**: 识别结果写入审查报告的 Stage 字段
- **追踪**: 通过审查报告追踪

## D-14: 部署约束
- **运行环境**: Claude Code CLI
- **依赖**: 无外部依赖，只依赖文件系统

## D-15: 依赖关系
- **上游依赖**: 迭代目录结构
- **下游依赖**: FT-001（核心框架）、FT-003（对齐检查）
- **Feature 依赖**: 无（独立组件）

## D-16: 实现映射
- **入口文件**: `skills/pb-review-v2/SKILL.md`（Workflow 第 1 步）
- **核心逻辑**: 基于文件存在性判断阶段
- **配置文件**: 无（规则内建于 SKILL.md Strategy 层）
