# FT-013: powerby-asp-arch-codex-reviewer 重写

## 基本信息
- **Feature ID**: FT-013
- **功能名称**: powerby-asp-arch-codex-reviewer 重写
- **对应需求**: REQ-013
- **功能类型**: skill
- **优先级**: P0
- **状态**: final

---

## D-01: 功能标识
- **全局唯一 ID**: FT-013
- **入口类型**: Skill
- **入口路径**: `skills/powerby-asp-arch-codex-reviewer/SKILL.md`

## D-02: 输入规格
| 参数名 | 类型 | 必填 | 约束 | 默认值 | 示例 |
|--------|------|------|------|--------|------|
| architecture.md | file | 是 | 架构设计文档 | - | 技术选型、架构设计 |
| 审查轮次 | number | 是 | ≥1 | - | 2 |

## D-03: 前置条件
- [ ] skill-design-protocol.md 已定义
- [ ] asp-document-protocol.md 已定义
- [ ] codex exec 命令可用
- [ ] architecture.md 已生成
- [ ] 现有 powerby-asp-arch-codex-reviewer 代码可读

## D-04: 正常输出

**SKILL.md 结构（必须符合 11 section 标准结构）**：
```markdown
---
name: powerby-asp-arch-codex-reviewer
description: ASP 架构设计流程的 Codex 审查程序
---

## Purpose
## Success criteria
## Strategy
## Tools and capability boundaries
## Important facts and constraints
## Workflow
## Output format
## Resources
## Subtask / parallelism guidance
## Examples
## Safety
```

**通过 codex exec 调用**：
```bash
codex exec \
  -s read-only \
  -C {项目根目录} \
  -o arch_logs/round-{N}-codex.md \
  "架构审查提示词"
```

## D-05: 异常行为
| 错误码 | 触发条件 | 错误消息 |
|--------|---------|---------|
| CODEX_EXEC_FAILED | codex exec 执行失败 | codex exec 返回非零退出码 |
| OUTPUT_FILE_MISSING | 输出文件不存在 | 找不到审查报告文件 |
| MISSING_SECTION | SKILL.md 缺少必填 section | SKILL.md 缺少 Strategy 章节 |
| INVALID_STRUCTURE | 不符合 11 section 标准结构 | 必须遵循 skill-design-protocol.md |

## D-06: 边界值
- 审查轮次 ≥ 1
- Status 必须为 PASS 或 FAIL
- SKILL.md 必须包含 11 个标准 section

## D-07: 后置条件
- [ ] SKILL.md 遵循 11 section 标准结构
- [ ] 通过 skill-design-protocol.md 的十条核心原则 checklist
- [ ] 审查报告已生成到 arch_logs/
- [ ] Status 字段明确

## D-08: 副作用
- 重写 skills/powerby-asp-arch-codex-reviewer/SKILL.md
- 创建 arch_logs/round-{N}-codex.md

---

## D-17: Test Oracle

### 成功输出 Schema
- SKILL.md 包含 frontmatter（name, description）
- 包含 11 个标准 section
- 包含 Reviewer: Codex
- 包含 Round、Status 字段
- 包含三维检查结果

### 业务规则
- 必须在 read-only 沙箱中执行
- 本迭代不创建 scripts/，确定性约束直接收敛在 SKILL.md 与 references/
- 领域知识分层到 references/

## D-18: Fixture Contract

### 最小数据集
- skill-design-protocol.md
- asp-document-protocol.md
- 现有 powerby-asp-arch-codex-reviewer/SKILL.md
- architecture.md

### Mock 策略
| 依赖 | Mock 类型 | Mock 返回值 |
|------|----------|------------|
| codex exec | 命令 Mock | 成功/失败 |

## D-19: Test Case Groups

| 测试组 | 用例数 | 优先级 |
|--------|--------|--------|
| 1. 主成功路径（PASS） | 1 | P0 |
| 2. 主成功路径（FAIL） | 1 | P0 |
| 3. 11 section 结构校验 | 11 | P0 |
| 4. 十条原则 checklist | 10 | P0 |
| 5. codex exec 失败处理 | 3 | P0 |
| 6. 输出格式校验 | 2 | P0 |

## D-20: Coverage Claim
- **允许覆盖声明**: 否
- **阻塞原因**: 测试组数 < 8（实际 6 组，但用例数充足）

---

---

## D-09: 性能要求
- 无特殊性能要求；codex exec 为异步执行，超时由 codex 自身控制

## D-10: 安全要求
- 必须在 read-only 沙箱中执行（`-s read-only`），不得修改项目文件
- 审查报告不得包含硬编码凭证或密钥

## D-11: 并发要求
- 单用户单次调用，无并发场景

## D-12: 数据一致性
- 审查报告输出格式必须符合 arch_logs/ 目录约定（文件名：`round-{N}-codex.md`）
- 报告必须包含 Reviewer: Codex、Round、Status 字段
- Status 值限定为 PASS 或 FAIL，不允许其他值

## D-13: 可观测性
- codex exec 的标准输出和标准错误需被捕获记录
- 执行完成后输出审查结果摘要（PASS/FAIL + 问题数）

## D-14: 部署约束
- 部署路径：`skills/powerby-asp-arch-codex-reviewer/`
- 运行时依赖：codex CLI 工具已安装且 `codex exec` 命令可用
- 依赖 skill-design-protocol.md 和 asp-document-protocol.md 存在于项目根目录

## D-15: 依赖关系

### 上游依赖
| Feature ID | 依赖类型 | 描述 |
|-----------|---------|------|
| FT-001 | 协议依赖 | asp-document-protocol.md 定义审查报告格式规范 |
| FT-011 | 产出依赖 | 依赖 powerby-asp-architect 产出的 architecture.md 作为审查输入 |

### 下游被依赖
| Feature ID | 依赖类型 | 描述 |
|-----------|---------|------|
| FT-014 | 编排依赖 | ASP 流程编排依赖本 Skill 完成 Codex 审查阶段 |

## D-16: 实现映射

### 入口
- `skills/powerby-asp-arch-codex-reviewer/SKILL.md`

### 产出
- `arch_logs/round-{N}-codex.md`

### 引用
- `arch-audit-checklist-ref.md`

### 测试
- `tests/test_asp_document_upgrade.py::test_architecture_line_skills_use_feature_specs_instead_of_legacy_function_points`
