# FT-009: powerby-asp-codex-reviewer 重写

## 基本信息
- **Feature ID**: FT-009
- **功能名称**: powerby-asp-codex-reviewer 重写
- **对应需求**: REQ-009
- **功能类型**: skill
- **优先级**: P0
- **状态**: final

---

## D-01: 功能标识
- **全局唯一 ID**: FT-009
- **入口类型**: Skill
- **入口路径**: `skills/powerby-asp-codex-reviewer/SKILL.md`

## D-02: 输入规格
| 参数名 | 类型 | 必填 | 约束 | 默认值 | 示例 |
|--------|------|------|------|--------|------|
| 审查上下文文件路径 | array | 是 | 文件路径列表 | - | [proposal.md, spec.md] |
| 输出文件路径 | string | 是 | 绝对路径 | - | prd_logs/round-2-codex.md |
| 审查轮次 | number | 是 | ≥1 | - | 2 |

## D-03: 前置条件
- [ ] skill-design-protocol.md 已定义
- [ ] asp-document-protocol.md 已定义
- [ ] codex exec 命令可用
- [ ] 待审查文档已生成
- [ ] 现有 powerby-asp-codex-reviewer 代码可读

## D-04: 正常输出

**SKILL.md 结构（必须符合 11 section 标准结构）**：
```markdown
---
name: powerby-asp-codex-reviewer
description: ASP 自治式规格精炼的 Codex 审查程序
---

## Purpose
## Success criteria
## Strategy
## Tools and capability boundaries
## Important facts and constraints
## Workflow
## Output format
## Resources
## Subtask / parallelism guidance
## Examples
## Safety
```

**通过 codex exec 调用**：
```bash
codex exec \
  -s read-only \
  -C {项目根目录} \
  -o {输出文件路径} \
  "审查提示词"
```

## D-05: 异常行为
| 错误码 | 触发条件 | 错误消息 |
|--------|---------|---------|
| CODEX_EXEC_FAILED | codex exec 执行失败 | codex exec 返回非零退出码 |
| OUTPUT_FILE_MISSING | 输出文件不存在 | 找不到审查报告文件 |
| INVALID_FORMAT | 输出格式错误 | 缺少 Status 字段 |
| MISSING_SECTION | SKILL.md 缺少必填 section | SKILL.md 缺少 Strategy 章节 |
| INVALID_STRUCTURE | 不符合 11 section 标准结构 | 必须遵循 skill-design-protocol.md |

## D-06: 边界值
- 审查轮次 ≥ 1
- 输出文件必须存在
- Status 必须为 PASS 或 FAIL
- SKILL.md 必须包含 11 个标准 section

## D-07: 后置条件
- [ ] SKILL.md 遵循 11 section 标准结构
- [ ] 通过 skill-design-protocol.md 的十条核心原则 checklist
- [ ] 审查报告已生成到指定路径
- [ ] Status 字段明确
- [ ] 问题清单完整

## D-08: 副作用
- 重写 skills/powerby-asp-codex-reviewer/SKILL.md
- 创建 prd_logs/round-{N}-codex.md
- 调用外部 codex exec 命令

---

## D-17: Test Oracle

### 成功输出 Schema
- SKILL.md 包含 frontmatter（name, description）
- 包含 11 个标准 section
- 包含 Reviewer: Codex
- 包含 Round、Status 字段
- 包含三维检查结果
- 包含问题清单

### 业务规则
- 必须在 read-only 沙箱中执行
- 输出文件路径必须在迭代目录下
- 本迭代不创建 scripts/，确定性约束直接收敛在 SKILL.md 与 references/
- 领域知识分层到 references/

## D-18: Fixture Contract

### 最小数据集
- skill-design-protocol.md
- asp-document-protocol.md
- 现有 powerby-asp-codex-reviewer/SKILL.md
- proposal.md（至少 1 个 REQ）
- feature-specs/*.md（至少 1 个功能）

### Mock 策略
| 依赖 | Mock 类型 | Mock 返回值 |
|------|----------|------------|
| codex exec | 命令 Mock | 成功/失败/超时 |

## D-19: Test Case Groups

| 测试组 | 用例数 | 优先级 |
|--------|--------|--------|
| 1. 主成功路径（PASS） | 1 | P0 |
| 2. 主成功路径（FAIL） | 1 | P0 |
| 3. 11 section 结构校验 | 11 | P0 |
| 4. 十条原则 checklist | 10 | P0 |
| 5. codex exec 失败处理 | 3 | P0 |
| 6. 输出格式校验 | 2 | P0 |

## D-20: Coverage Claim
- **允许覆盖声明**: 否
- **阻塞原因**: 测试组数 < 8（实际 6 组，但用例数充足）

---

---

## D-09: 性能要求
- 无性能要求（AI prompt skill，无运行时性能约束）

## D-10: 安全要求
- 无安全要求（不涉及用户数据或认证）

## D-11: 并发要求
- 无并发要求（单会话执行）

## D-12: 数据一致性
- SKILL.md 的 11 个 section 必须与 skill-design-protocol.md 定义一致
- 审查报告格式必须包含 Reviewer: Codex、Round、Status 字段及三维检查结果
- 审查报告的问题清单严重度分级（BLOCKER/MAJOR/MINOR）必须与 asp-document-protocol.md v1.1.0 定义一致

## D-13: 可观测性
- 无可观测性要求（Skill 执行由 Claude Code ��主管理）

## D-14: 部署约束
- Skill 文件存放路径：`skills/powerby-asp-codex-reviewer/`
- 目录结构：`SKILL.md` + `references/`

## D-15: 依赖关系

### 上游依赖
| Feature ID | 依赖类型 | 描述 |
|-----------|---------|------|
| FT-001 | protocol | 依赖 asp-document-protocol.md v1.1.0 协议定义 |
| FT-007 | input | 依赖 powerby-asp-product 产出的文档作为审查输入 |

### 下游被依赖
| Feature ID | 依赖类型 | 描述 |
|-----------|---------|------|
| FT-014 | orchestration | ASP 流程编排依赖此 skill |

## D-16: 实现映射

### 入口
- `skills/powerby-asp-codex-reviewer/SKILL.md`

### 产出
- `skills/powerby-asp-codex-reviewer/SKILL.md`（重写后的 11 section 标准结构）
- `skills/powerby-asp-codex-reviewer/references/audit-checklist-ref.md`

### 测试
- `tests/test_asp_document_upgrade.py::test_product_line_skills_use_new_document_protocol`
