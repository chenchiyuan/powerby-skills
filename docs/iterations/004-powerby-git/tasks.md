# PowerBy Git 任务计划

> 版本: v1.0.0
> 创建日期: 2026-01-12
> 架构文档: architecture.md

---

## 任务概览

| 阶段 | 任务数 | 预计周期 |
|------|--------|----------|
| 阶段一：基础设施 | 3 | 1天 |
| 阶段二：核心功能 | 5 | 2天 |
| 阶段三：Git Hooks | 3 | 1天 |
| 阶段四：集成测试 | 2 | 1天 |
| **合计** | **13** | **5天** |

---

## 阶段一：基础设施搭建

### 任务 1.1：创建项目目录结构

**目标**: 创建 powerby-git 角色的目录结构和基础文件

**验收标准**:
- [ ] `skills/powerby-git/` 目录存在
- [ ] `skills/powerby-git/src/` 源代码目录
- [ ] `skills/powerby-git/tests/` 测试目录
- [ ] `skills/powerby-git/templates/` 模板目录

**依赖**: 无

**工作量**: 0.5天

**详细步骤**:
```
1. 创建目录结构
   skills/powerby-git/
   ├── src/
   │   ├── commands/     # 命令实现
   │   ├── core/         # 核心模块
   │   ├── hooks/        # Git Hooks
   │   └── utils/        # 工具函数
   ├── tests/
   │   ├── unit/         # 单元测试
   │   └── integration/  # 集成测试
   └── templates/        # 模板文件
```

---

### 任务 1.2：创建 package.json 和依赖配置

**目标**: 配置项目依赖和脚本

**验收标准**:
- [ ] `package.json` 包含正确的依赖
- [ ] 包含 `powerby-git` 命令脚本
- [ ] 包含测试和构建脚本

**依赖**: 任务 1.1

**工作量**: 0.5天

**详细步骤**:
```
1. 配置依赖
   - commander: CLI 命令框架
   - chalk: 命令行着色
   - inquirer: 交互式输入
   - simple-git: Git 操作封装

2. 配置脚本
   - "start": powerby-git
   - "test": jest
   - "lint": eslint
```

---

### 任务 1.3：实现基础框架和错误处理

**目标**: 创建 CLI 入口和基础框架

**验收标准**:
- [ ] CLI 入口正确解析参数
- [ ] 统一的错误处理和输出格式
- [ ] 支持 `--help` 和 `--version`

**依赖**: 任务 1.2

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 CLI 入口 (index.js)
   - 参数解析
   - 命令路由
   - 帮助信息

2. 实现错误处理
   - 统一的错误码体系
   - 错误输出格式
   - 建议解决方案

3. 实现输出格式
   - 成功信息
   - 进度提示
   - 错误信息
```

---

## 阶段二：核心功能实现

### 任务 2.1：实现分支命名验证器

**目标**: 实现分支名称格式验证

**验收标准**:
- [ ] 验证 `feature/{name}` 格式
- [ ] 验证 `bugfix/{name}` 格式
- [ ] 验证 `hotfix/{version}-{name}` 格式
- [ ] 验证 `release/{version}` 格式
- [ ] 拒绝无效格式

**依赖**: 任务 1.3

**工作量**: 0.5天

**详细步骤**:
```
1. 创建分支验证器 (src/core/branch-validator.js)
   - 正则表达式验证
   - 类型检查
   - 格式规范化

2. 测试用例
   - 有效分支名测试
   - 无效分支名测试
   - 边界情况测试
```

---

### 任务 2.2：实现分支创建命令

**目标**: 实现 `powerby-git start` 命令

**验收标准**:
- [ ] 支持 `--type=feature` 参数
- [ ] 支持 `--type=bugfix` 参数
- [ ] 支持 `--name` 参数
- [ ] 自动生成规范分支名
- [ ] 执行 `git checkout -b` 创建分支
- [ ] 检查分支是否已存在

**依赖**: 任务 2.1

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 start 命令 (src/commands/start.js)
   - 解析参数
   - 验证分支名
   - 检查分支是否存在
   - 执行 git checkout -b
   - 返回成功信息

2. 集成模板
   - feature 模板
   - bugfix 模板
```

---

### 任务 2.3：实现分支列表命令

**目标**: 实现 `powerby-git list` 命令

**验收标准**:
- [ ] 列出所有分支
- [ ] 支持 `--merged` 过滤
- [ ] 支持 `--unmerged` 过滤
- [ ] 显示分支状态信息

**依赖**: 任务 2.1

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 list 命令 (src/commands/list.js)
   - 获取分支列表
   - 过滤已合并/未合并分支
   - 格式化输出

2. 显示信息
   - 分支名
   - 最近提交
   - 是否已合并
```

---

### 任务 2.4：实现文件白名单验证器

**目标**: 实现文件合规性检查的核心逻辑

**验收标准**:
- [ ] 验证三类合法文件
- [ ] 识别临时文件
- [ ] 支持自定义白名单
- [ ] 返回详细的检查结果

**依赖**: 无

**工作量**: 1天

**详细步骤**:
```
1. 实现白名单验证器 (src/core/file-whitelist.js)
   - 业务代码规则
   - PowerBy 文档规则
   - 项目配置文件规则

2. 实现临时文件识别
   - *.tmp / *.log / *.bak
   - __pycache__ / node_modules
   - .DS_Store / .env

3. 实现检查报告生成
   - 合法文件列表
   - 临时文件列表
   - 建议操作
```

---

### 任务 2.5：实现提交信息验证器

**目标**: 实现提交信息格式验证

**验收标准**:
- [ ] 验证 `{type}({scope}): {message}` 格式
- [ ] 支持标准类型 (feat, fix, docs, etc.)
- [ ] 验证类型和范围格式
- [ ] 返回详细验证结果

**依赖**: 无

**工作量**: 0.5天

**详细步骤**:
```
1. 实现提交信息验证器 (src/core/commit-validator.js)
   - 正则表达式验证
   - 类型检查
   - 范围验证

2. 支持的类型
   - feat, fix, docs, style
   - refactor, test, chore
```

---

## 阶段三：Git Hooks 集成

### 任务 3.1：实现 pre-commit 钩子

**目标**: 实现提交时的自动检查

**验收标准**:
- [ ] 安装 pre-commit 钩子
- [ ] 拦截不合规提交
- [ ] 显示错误信息和建议
- [ ] 支持跳过检查（可选）

**依赖**: 任务 2.4, 2.5

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 pre-commit 钩子 (src/hooks/pre-commit.js)
   - 获取待提交文件
   - 执行文件白名单检查
   - 验证提交信息
   - 返回检查结果

2. 实现钩子安装
   - 创建 .git/hooks/pre-commit
   - 设置执行权限
```

---

### 任务 3.2：实现 pre-merge 钩子

**目标**: 实现合并前的全量检查

**验收标准**:
- [ ] 安装 pre-merge 钩子
- [ ] 全量扫描工作区文件
- [ ] 检查 PowerBy 文档完整性
- [ ] 验证无临时文件

**依赖**: 任务 2.4

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 pre-merge 钩子 (src/hooks/pre-merge.js)
   - 获取所有工作区文件
   - 执行全量白名单检查
   - 检查文档完整性
   - 返回检查结果

2. 实现钩子安装
   - 创建 .git/hooks/pre-merge
   - 设置执行权限
```

---

### 任务 3.3：实现检查命令

**目标**: 实现 `powerby-git check` 命令

**验收标准**:
- [ ] 支持 `--type=commit` 检查
- [ ] 支持 `--type=merge` 检查
- [ ] 支持 `--type=full` 检查
- [ ] 返回详细的检查报告

**依赖**: 任务 2.4, 2.5, 3.1, 3.2

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 check 命令 (src/commands/check.js)
   - commit: 检查待提交文件
   - merge: 全量扫描
   - full: 完整检查

2. 输出报告格式
   - 检查类型
   - 文件统计
   - 问题列表
   - 建议操作
```

---

## 阶段四：集成与测试

### 任务 4.1：实现分支清理功能

**目标**: 实现 `powerby-git cleanup` 命令

**验收标准**:
- [ ] 列出待清理分支
- [ ] 支持 `--dry-run` 预览模式
- [ ] 支持 `--force` 强制执行
- [ ] 验证分支已合并后再删除

**依赖**: 任务 2.3

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 cleanup 命令 (src/commands/cleanup.js)
   - 获取已合并分支
   - dry-run: 预览不删除
   - force: 执行删除

2. 安全检查
   - 检查是否已合并
   - 检查是否有未推送提交
   - 确认删除操作
```

---

### 任务 4.2：实现状态查看命令

**目标**: 实现 `powerby-git status` 命令

**验收标准**:
- [ ] 显示当前分支
- [ ] 显示待提交变更
- [ ] 显示识别的临时文件
- [ ] 显示检查建议

**依赖**: 任务 2.4

**工作量**: 0.5天

**详细步骤**:
```
1. 实现 status 命令 (src/commands/status.js)
   - 获取当前分支
   - 获取变更文件
   - 执行白名单检查
   - 生成状态报告
```

---

### 任务 4.3：编写测试用例

**目标**: 为所有核心功能编写测试

**验收标准**:
- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试覆盖主要流程
- [ ] 所有测试通过

**依赖**: 所有功能任务

**工作量**: 1天

**测试用例**:
```
单元测试:
- 分支命名验证
- 文件白名单验证
- 提交信息验证
- 错误处理

集成测试:
- start 命令完整流程
- check 命令完整流程
- cleanup 命令完整流程
- Git Hooks 触发
```

---

## 任务依赖关系

```
阶段一：基础设施
├── 1.1 目录结构 ──┐
├── 1.2 依赖配置 ──┤
└── 1.3 框架实现 ──┘

阶段二：核心功能
├── 2.1 分支验证器 ──┐
├── 2.2 start 命令 ──┤
├── 2.3 list 命令 ──┤
├── 2.4 白名单验证 ──┤
└── 2.5 提交验证 ────┘

阶段三：Git Hooks
├── 3.1 pre-commit ──┐
├── 3.2 pre-merge ──┤
└── 3.3 check 命令 ──┘

阶段四：集成测试
├── 4.1 cleanup 命令 ──┐
├── 4.2 status 命令 ──┤
└── 4.3 编写测试 ──────┘
```

---

## 每日任务安排

### Day 1
- [ ] 1.1 创建项目目录结构
- [ ] 1.2 创建 package.json
- [ ] 1.3 实现基础框架

### Day 2
- [ ] 2.1 实现分支命名验证器
- [ ] 2.2 实现分支创建命令
- [ ] 2.3 实现分支列表命令

### Day 3
- [ ] 2.4 实现文件白名单验证器
- [ ] 2.5 实现提交信息验证器
- [ ] 3.1 实现 pre-commit 钩子

### Day 4
- [ ] 3.2 实现 pre-merge 钩子
- [ ] 3.3 实现检查命令
- [ ] 4.1 实现分支清理功能

### Day 5
- [ ] 4.2 实现状态查看命令
- [ ] 4.3 编写测试用例
- [ ] 整体集成测试

---

> 任务文档路径: `docs/iterations/004-powerby-git/tasks.md`
