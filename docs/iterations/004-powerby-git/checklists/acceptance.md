# PowerBy Git 验收清单

> 版本: v1.0.0
> 创建日期: 2026-01-12
> 任务文档: tasks.md

---

## 验收概览

| 验收项 | 总数 | 已验收 | 通过率 |
|--------|------|--------|--------|
| 功能验收 | 25 | 0 | 0% |
| 质量检查 | 15 | 0 | 0% |
| 测试要求 | 10 | 0 | 0% |
| **合计** | **50** | **0** | **0%** |

---

## 一、功能验收标准

### 1.1 分支创建功能

| 序号 | 验收项 | 优先级 | 状态 | 验收说明 |
|------|--------|--------|------|----------|
| F-001 | 支持 `feature/` 前缀 | P0 | ☐ | feature/user-authentication 格式 |
| F-002 | 支持 `bugfix/` 前缀 | P0 | ☐ | bugfix/login-timeout 格式 |
| F-003 | 支持 `hotfix/` 前缀 | P1 | ☐ | hotfix/v1.2.3-security 格式 |
| F-004 | 支持 `release/` 前缀 | P1 | ☐ | release/v2.0.0 格式 |
| F-005 | 自动生成规范分支名 | P0 | ☐ | 根据类型和名称生成 |
| F-006 | 检查分支是否已存在 | P0 | ☐ | 避免重复创建 |
| F-007 | 执行 `git checkout -b` | P0 | ☐ | 正确创建分支 |

**验收方法**:
```bash
# 测试 feature 分支创建
powerby-git start --type=feature --name=user-authentication
# 应创建并切换到 feature/user-authentication

# 测试 bugfix 分支创建
powerby-git start --type=bugfix --name=login-timeout
# 应创建并切换到 bugfix/login-timeout

# 测试重复创建
powerby-git start --type=feature --name=user-authentication
# 应提示分支已存在
```

---

### 1.2 分支列表功能

| 序号 | 验收项 | 优先级 | 状态 | 验收说明 |
|------|--------|--------|------|----------|
| F-008 | 列出所有本地分支 | P0 | ☐ | `git branch` 结果 |
| F-009 | 支持 `--merged` 过滤 | P1 | ☐ | 只显示已合并分支 |
| F-010 | 支持 `--unmerged` 过滤 | P1 | ☐ | 只显示未合并分支 |
| F-011 | 显示分支状态信息 | P1 | ☐ | 显示是否已合并 |

**验收方法**:
```bash
# 列出所有分支
powerby-git list

# 列出已合并分支
powerby-git list --merged

# 列出未合并分支
powerby-git list --unmerged
```

---

### 1.3 分支清理功能

| 序号 | 验收项 | 优先级 | 状态 | 验收说明 |
|------|--------|--------|------|----------|
| F-012 | 识别已合并分支 | P0 | ☐ | 检测分支是否已合并 |
| F-013 | `--dry-run` 预览模式 | P0 | ☐ | 显示但不删除 |
| F-014 | `--force` 强制执行 | P1 | ☐ | 跳过确认直接删除 |
| F-015 | 安全删除验证 | P0 | ☐ | 确保分支已合并 |

**验收方法**:
```bash
# 预览待清理分支
powerby-git cleanup --dry-run
# 应显示已合并分支列表，不执行删除

# 执行清理
powerby-git cleanup --force
# 应删除所有已合并分支
```

---

### 1.4 提交检查功能

| 序号 | 验收项 | 优先级 | 状态 | 验收说明 |
|------|--------|--------|------|----------|
| F-016 | 验证提交信息格式 | P0 | ☐ | `{type}({scope}): message` |
| F-017 | 支持标准类型 | P0 | ☐ | feat, fix, docs, style, refactor, test, chore |
| F-018 | 验证文件白名单 | P0 | ☐ | 检查文件是否合法 |
| F-019 | 识别临时文件 | P0 | ☐ | *.tmp, *.log, *.bak 等 |
| F-020 | 返回详细错误信息 | P1 | ☐ | 包含建议解决方案 |

**验收方法**:
```bash
# 有效提交
git commit -m "feat(auth): add login"
# 应通过检查

# 无效提交
git commit -m "added login feature"
# 应提示格式错误

# 临时文件检查
powerby-git check --type=commit
# 应标记 debug.log 为临时文件
```

---

### 1.5 合并检查功能

| 序号 | 验收项 | 优先级 | 状态 | 验收说明 |
|------|--------|--------|------|----------|
| F-021 | 全量扫描工作区 | P0 | ☐ | 检查所有文件 |
| F-022 | 验证 PowerBy 文档 | P1 | ☐ | prd.md, task.md 等 |
| F-023 | 验证无临时文件 | P0 | ☐ | *.tmp, *.log 等 |
| F-024 | 阻止不合规合并 | P0 | ☐ | 检查不通过则阻止 |

**验收方法**:
```bash
# 合并前检查
powerby-git check --type=merge
# 应扫描所有文件，返回合规性报告

# 临时文件存在时
touch debug.log
powerby-git check --type=merge
# 应提示删除 debug.log 并阻止合并
```

---

### 1.6 状态查看功能

| 序号 | 验收项 | 优先级 | 状态 | 验收说明 |
|------|--------|--------|------|----------|
| F-025 | 显示当前分支 | P0 | ☐ | `git branch` 结果 |
| F-026 | 显示待提交变更 | P1 | ☐ | `git status` 结果 |
| F-027 | 显示临时文件 | P1 | ☐ | 识别临时文件 |
| F-028 | 显示检查建议 | P2 | ☐ | 建议下一步操作 |

**验收方法**:
```bash
powerby-git status
# 应显示：
# - 当前分支名
# - 待提交文件列表
# - 识别的临时文件
# - 改进建议
```

---

## 二、质量检查清单

### 2.1 代码质量

| 序号 | 检查项 | 优先级 | 状态 | 说明 |
|------|--------|--------|------|------|
| Q-001 | ESLint 检查通过 | P0 | ☐ | 无代码风格问题 |
| Q-002 | 无 console.log 残留 | P1 | ☐ | 使用日志框架 |
| Q-003 | 注释完整 | P2 | ☐ | 复杂逻辑有注释 |
| Q-004 | 错误处理完善 | P0 | ☐ | 所有异步操作有错误处理 |
| Q-005 | 统一的输出格式 | P1 | ☐ | 成功/错误/信息格式统一 |

**检查方法**:
```bash
npm run lint
# 应无错误输出
```

---

### 2.2 错误处理

| 序号 | 检查项 | 优先级 | 状态 | 说明 |
|------|--------|--------|------|------|
| Q-006 | 无效参数处理 | P0 | ☐ | 无效 type/name 给出错误 |
| Q-007 | Git 操作失败处理 | P0 | ☐ | git 命令失败给出提示 |
| Q-008 | 权限不足处理 | P1 | ☐ | 提示权限问题 |
| Q-009 | 文件不存在处理 | P1 | ☐ | 提示文件不存在 |

**错误码体系**:
```
E001: 分支已存在
E002: 无效的分支名称
E003: 不在分支上
E004: 临时文件未清理
E005: 文档不完整
E006: 合并冲突
E007: Git 操作失败
E008: 权限不足
```

---

### 2.3 文档质量

| 序号 | 检查项 | 优先级 | 状态 | 说明 |
|------|--------|--------|------|------|
| Q-010 | README 完整 | P0 | ☐ | 包含快速开始和示例 |
| Q-011 | 命令帮助信息 | P0 | ☐ | --help 输出完整 |
| Q-012 | 错误信息有建议 | P1 | ☐ | 错误提示包含解决方案 |
| Q-013 | 示例代码正确 | P1 | ☐ | 文档示例可运行 |

**检查方法**:
```bash
powerby-git --help
# 应显示完整的帮助信息

powerby-git start --help
# 应显示 start 命令的帮助信息
```

---

### 2.4 Git Hooks 质量

| 序号 | 检查项 | 优先级 | 状态 | 说明 |
|------|--------|--------|------|------|
| Q-014 | 钩子安装成功 | P0 | ☐ | .git/hooks/ 存在 |
| Q-015 | 钩子可执行 | P0 | ☐ | 有执行权限 |
| Q-016 | 钩子退出码正确 | P0 | ☐ | 检查通过返回 0 |

**检查方法**:
```bash
ls -la .git/hooks/
# 应显示 pre-commit 和 pre-merge
```

---

## 三、测试要求

### 3.1 单元测试

| 序号 | 测试项 | 覆盖率 | 状态 | 说明 |
|------|--------|--------|------|------|
| T-001 | 分支命名验证 | >90% | ☐ | 有效/无效格式测试 |
| T-002 | 文件白名单验证 | >90% | ☐ | 合法/临时文件测试 |
| T-003 | 提交信息验证 | >90% | ☐ | 格式验证测试 |
| T-004 | 错误处理 | >80% | ☐ | 异常场景测试 |

**运行测试**:
```bash
npm test
# 应显示测试通过
```

---

### 3.2 集成测试

| 序号 | 测试项 | 状态 | 说明 |
|------|--------|------|------|
| T-005 | start 命令完整流程 | ☐ | 创建分支到切换 |
| T-006 | list 命令完整流程 | ☐ | 列出并过滤分支 |
| T-007 | check 命令完整流程 | ☐ | 检查并返回结果 |
| T-008 | cleanup 命令完整流程 | ☐ | 预览到删除 |
| T-009 | pre-commit 钩子触发 | ☐ | 提交时检查 |
| T-010 | pre-merge 钩子触发 | ☐ | 合并时检查 |

---

## 四、验收流程

### 4.1 自检清单

开发完成后的自检步骤：

```bash
# 1. 运行 lint
npm run lint

# 2. 运行测试
npm test

# 3. 检查覆盖率
npm run test:coverage

# 4. 测试命令
powerby-git --help
powerby-git start --help
powerby-git check --help
powerby-git cleanup --help

# 5. 测试功能
powerby-git start --type=feature --name=test-feature
git add .
git commit -m "feat(test): test commit"
powerby-git status
powerby-git check --type=commit
powerby-git cleanup --dry-run
```

### 4.2 验收签字

| 角色 | 签名 | 日期 |
|------|------|------|
| 开发 | ☐ | |
| 审查 | ☐ | |
| 验收 | ☐ | |

---

## 五、已知限制

1. **仅支持本地 Git 仓库**: 不支持远程分支操作
2. **不支持 Windows cmd**: 需要 PowerShell 或 WSL
3. **临时文件识别有限**: 自定义临时文件后缀需要配置

---

> 验收清单路径: `docs/iterations/004-powerby-git/checklists/acceptance.md`
